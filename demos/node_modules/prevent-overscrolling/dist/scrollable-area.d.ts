export declare class ScrollableArea {
    Element: HTMLElement;
    private touchStart;
    private hasFocus;
    constructor(Element: HTMLElement);
    SetEventListeners(enable: boolean): void;
    private handleTouchStart(event);
    private handleClick(event);
    private handleOverScroll(event);
}
