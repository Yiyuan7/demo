"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var scrollable_area_1 = require("./scrollable-area");
var scrollableAreas = [];
function ScrollTo(target, options) {
    return new Promise(function (resolve) {
        var scrollContainer = options ? options.scrollContainer || window : window;
        var scrollableArea = scrollableAreas.find(function (a) { return a.element === scrollContainer; });
        if (!scrollableArea) {
            scrollableArea = {
                element: scrollContainer,
                class: new scrollable_area_1.ScrollableArea(scrollContainer),
            };
            scrollableAreas.push(scrollableArea);
        }
        scrollableArea.class.ScrollToTarget(target, options).then(resolve);
    });
}
exports.ScrollTo = ScrollTo;
