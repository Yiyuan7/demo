import { Position, IScrollableAreaOptions } from './models';
export declare class ScrollableArea {
    private scrollContainer;
    private ticking;
    private _scrolling;
    private scrollFrom;
    private scrollTo;
    private duration;
    private timestamp;
    private scrollX;
    private scrollY;
    private easing;
    private resolve;
    private userScrollingPrevented;
    constructor(scrollContainer: HTMLElement | Window);
    ScrollToTarget(target: Position | HTMLElement, options?: IScrollableAreaOptions): Promise<void>;
    private scrolling;
    private setScrollPosition();
    private addEventListeners();
    private removeEventListeners();
    private cancelScroll();
    private tick();
    private onScrollEnd();
    private scroll();
    private calculateNextPosition(index, elapsed);
}
